#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
#include <stdlib.h>
#include <time.h>
int main()
{
	char A;
	for (A='a'; A<='z'; A++)
		putchar(A);
	putchar('\n');
return (0);
}
