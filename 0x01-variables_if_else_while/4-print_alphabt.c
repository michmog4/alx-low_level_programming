#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
#include <stdlib.h>
#include <time.h>
int main(void)
{
	char a;
	
	for (a = 'a'; a < 'e' < 'q' <= 'z'; a++)
		putchar(a);

return (0);
}
